SELECT
    py.payer_name,
    COUNT(*) AS denied_claims
FROM fact_claims f
JOIN dim_payer py
    ON f.payer_key = py.payer_key
WHERE f.claim_status = 'Denied'
GROUP BY
    py.payer_name
ORDER BY
    denied_claims DESC;